<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="@dimen/actor_item_width"
    android:layout_height="@dimen/actor_item_height"
    xmlns:tools="http://schemas.android.com/tools"
    >

    <ImageView
        android:id="@+id/ivActorImage"
        android:layout_width="match_parent"
        android:layout_height="196dp"

        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@drawable/jason_statham"
        android:contentDescription="@string/str_actor_img_description" />

    <TextView
        android:id="@+id/tvActorName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/actor_item_text_margin_top"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:text="@string/str_first_actor_name"
        android:textStyle="bold"
        android:fontFamily="@font/actor_name_font"
        android:textSize="@dimen/actor_name_text_size"
        android:textColor="@color/custom_text"
        app:layout_constraintTop_toBottomOf="@+id/ivActorImage"/>


</androidx.constraintlayout.widget.ConstraintLayout>